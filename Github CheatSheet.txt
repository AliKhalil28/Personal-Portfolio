// For pushing data in github
git config --global user.name Ali
git config --global user.email "ak126pk@gmail.com"
touch .gitignore
git init
git add .
git commit -m "Initail commit"
git status
git remote add origin (SSH)
git push origin master
ssh-keygen -t ed25519 -C "ak126pk@gamil.com"
cat /c/Users/Captain/.ssh/id_ed25519.pub
git push origin master

//For updating data in github
git status
git add .
git commit -m "(comment)"
git pull
git push origin master

//For recovering data
git status
git checkout filename
or
git checkout -f

// For checking commit
git log
or 
git log -p -1

// Checking difference b/w previous and this stage
git diff
or
git diff --staged

// For removing file 
git rm --cached filename //Delete from github
git rm filename // Delete from harddisk

// Branches
git branch // For checking number of branches
git branch branchname // For creating a new branch
git chechout branchname // For moving to that branch 
git merge branchname // For merge 2 branches
git checkout -b branchname // For Creating and moving in the new branch


